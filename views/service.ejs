<%- include("_header") -%>

<h1>Hello <%= name %>!</h1>
<h2>We are happy to see you here with us. Let's work together! </h2>
<p>We usually provided mentioned services:</p>
<ul>
    <% othData.serviceList.forEach(service => { %>
        <li><b>
            <%= service.name %> </b> - <%= service.desc %>
        </li>
    <% }); %>
</ul>
<p>Thank you for being with us for <b><%= othData.years %> </b>
    <% if(othData.years > 1) { %> 
        years 
    <% } else { %> 
        year 
    <% };%>
</p>

<%- include("_footer") -%>